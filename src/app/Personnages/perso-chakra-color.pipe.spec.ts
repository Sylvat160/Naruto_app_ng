import { PersoChakraColorPipe } from './perso-chakra-color.pipe';

describe('PersoChakraColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PersoChakraColorPipe();
    expect(pipe).toBeTruthy();
  });
});
